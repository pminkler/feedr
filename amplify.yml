version: 1
backend:
  phases:
    build:
      commands:
        - npm install -g pnpm
        - mkdir -p /root/.local/share/pnpm
        - export PNPM_HOME="/root/.local/share/pnpm"
        - export PATH="$PNPM_HOME:$PATH"
        - export SHELL="/bin/bash"
        - pnpm install --no-frozen-lockfile
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - mkdir -p /root/.local/share/pnpm
        - export PNPM_HOME="/root/.local/share/pnpm"
        - export PATH="$PNPM_HOME:$PATH"
        - export SHELL="/bin/bash"
        - pnpm install --no-frozen-lockfile
        # Run unit tests and type checks as part of the build process
        - pnpm typecheck
        - pnpm test
        - pnpm test:verify-failures
    build:
      commands:
        - pnpm generate
  artifacts:
    baseDirectory: .amplify-hosting/static
    files:
      - '**/*'
test:
  phases:
    preTest:
      commands:
        - npm install -g pnpm
        - mkdir -p /root/.local/share/pnpm
        - export PNPM_HOME="/root/.local/share/pnpm"
        - export PATH="$PNPM_HOME:$PATH"
        - export SHELL="/bin/bash"
        # Clean installation to avoid file system conflicts
        - rm -rf node_modules
        - pnpm install --no-frozen-lockfile --prefer-offline
        - npm install -g wait-on pm2
        - pnpm install -D mocha mochawesome mochawesome-merge mochawesome-report-generator
        - pnpm build # Build the app first
        - pm2 start "pnpm preview" --name nuxt-app
        - wait-on --timeout 300000 http://localhost:3000
    test:
      commands:
        - 'npx cypress run --reporter mochawesome --reporter-options "reportDir=cypress/report/mochawesome-report,overwrite=false,html=false,json=true,timestamp=mmddyyyy_HHMMss"'
    postTest:
      commands:
        - npx mochawesome-merge cypress/report/mochawesome-report/mochawesome*.json > cypress/report/mochawesome.json
        - pm2 delete nuxt-app
  artifacts:
    baseDirectory: cypress
    configFilePath: '**/mochawesome.json'
    files:
      - '**/*.png'
      - '**/*.mp4'
cache:
  paths:
    - .pnpm-store/**/*
    - node_modules/**/*